# aegis

![Version: 0.2.0](https://img.shields.io/badge/Version-0.2.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: 0.2.0](https://img.shields.io/badge/AppVersion-0.2.0-informational?style=flat-square)

Vendor-agnostic secrets broker and PAM gateway. Scoped API keys per team, any vault, every action logged.

**Homepage:** <https://github.com/gustav0thethird/Aegis>

<!-- This file is generated by helm-docs from README.md.gotmpl and the
     "# --" comments in values.yaml. Edit those, then run `make helm-docs`. -->

## Installing

The chart is published as a signed OCI artifact alongside each release:

```bash
helm install aegis oci://ghcr.io/gustav0thethird/charts/aegis --version 0.2.0 \
  --namespace aegis --create-namespace \
  --set secret.existingSecret=aegis-credentials \
  --set auth.existingSecret=aegis-auth-json \
  --set migrations.hookProvider=helm
```

Two Secrets must exist in the namespace before installing:

| Secret | Keys | Purpose |
|---|---|---|
| `secret.existingSecret` | `database-url`, `redis-url`, `admin-password`, optionally `secret-key` | Runtime configuration |
| `auth.existingSecret` | `auth.json` (or `auth.secretKey`) | Vault backend credentials the broker brokers with |

`migrations.hookProvider` defaults to `argocd`; set it to `helm` for plain
`helm install` / `helm upgrade` so the migration Job runs as a pre-install hook.

Verify the chart's signature (keyless, bound to the release workflow):

```bash
cosign verify oci://ghcr.io/gustav0thethird/charts/aegis:0.2.0 \
  --certificate-identity-regexp 'https://github.com/gustav0thethird/Aegis/' \
  --certificate-oidc-issuer https://token.actions.githubusercontent.com
```

## Upgrading

```bash
helm upgrade aegis oci://ghcr.io/gustav0thethird/charts/aegis --version <new> --namespace aegis --reuse-values
```

The migration Job runs ahead of the rollout; a failed migration fails the
upgrade before any new pod starts.

## Maintainers

| Name | Email | Url |
| ---- | ------ | --- |
| gustav0thethird |  | <https://github.com/gustav0thethird> |

## Source Code

* <https://github.com/gustav0thethird/Aegis>

## Requirements

Kubernetes: `>=1.25.0-0`

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| affinity | object | `{}` | Affinity rules for broker pods. |
| auth.authPath | string | `"/etc/aegis/auth.json"` | Path the file is mounted at inside the container. |
| auth.existingSecret | string | `""` | Name of an existing Secret holding auth.json. Required. |
| auth.secretKey | string | `"auth.json"` | Key within that Secret that holds the file. |
| autoscaling.enabled | bool | `false` | Manage replica count with a HorizontalPodAutoscaler. |
| autoscaling.maxReplicas | int | `6` | Maximum replicas. |
| autoscaling.minReplicas | int | `2` | Minimum replicas. |
| autoscaling.targetCPUUtilizationPercentage | int | `75` | Target average CPU utilisation. |
| config.extraEnv | object | `{}` | Additional non-sensitive environment variables, rendered into the ConfigMap. |
| config.logDestinations | string | `"stdout"` | Comma-separated audit log destinations: stdout, splunk, s3, datadog. |
| config.rateLimitFailMode | string | `"open"` | Behaviour when Redis is unreachable: open serves without enforcing limits (availability first), closed rejects requests (security first). |
| config.rateLimitRpm | string | `"60"` | Per-key requests per minute. |
| config.webhookAllowedHosts | string | `""` | Comma-separated host allowlist for outbound webhook and alert URLs. Empty means any public host; private and loopback targets are always rejected. |
| config.webhookAllowedSchemes | string | `"https"` | Comma-separated URL schemes accepted for outbound webhook and alert URLs. |
| externalSecrets.clusterSecretStore.apiKeySecret | object | `{"key":"api-key","name":"aegis-eso-credentials","namespace":"external-secrets"}` | Secret holding the Aegis team API key ESO authenticates with. |
| externalSecrets.clusterSecretStore.changeNumber | string | `"ESO-SYNC"` | Sent as X-Change-Number on every ESO fetch. A standing identifier for automated sync traffic; set it to whatever your change process expects, or turn change_number_required off for this path. |
| externalSecrets.clusterSecretStore.enabled | bool | `false` | Emit a ClusterSecretStore pointing at this release so workloads can pull brokered secrets through the External Secrets Operator. |
| externalSecrets.clusterSecretStore.name | string | `"aegis"` | Name of the ClusterSecretStore. |
| fullnameOverride | string | `""` | Override the full release name used in resource names. |
| image.pullPolicy | string | `"IfNotPresent"` | Image pull policy. |
| image.repository | string | `"ghcr.io/gustav0thethird/aegis"` | Image repository. Release images are multi-arch (amd64, arm64), signed with Sigstore and carry an SBOM. |
| image.tag | string | `""` | Image tag. Defaults to "v" + .Chart.AppVersion, matching release tags. |
| imagePullSecrets | list | `[]` | Pull secrets for a private mirror of the image. |
| ingress.annotations | object | `{}` | Annotations for the Ingress (cert-manager issuer, rate limits, ...). |
| ingress.className | string | `""` | IngressClass name. |
| ingress.enabled | bool | `false` | Expose the broker through an Ingress. |
| ingress.hosts | list | `[{"host":"aegis.example.com","paths":[{"path":"/","pathType":"Prefix"}]}]` | Hosts and paths routed to the broker. |
| ingress.tls | list | `[]` | TLS configuration for the Ingress. |
| migrations.backoffLimit | int | `3` | Job retries before the rollout is failed. |
| migrations.enabled | bool | `true` | Run alembic upgrade head in a Job ahead of each rollout. |
| migrations.hookProvider | string | `"argocd"` | How the Job is ordered before the Deployment: argocd (PreSync hook), helm (pre-install/pre-upgrade hook) or none (plain Job). |
| migrations.resources | object | `{"requests":{"cpu":"50m","memory":"128Mi"}}` | Resources for the migration Job. |
| nameOverride | string | `""` | Override the chart name used in resource names. |
| networkPolicy.allowedNamespaces | list | `[]` | Namespaces allowed to reach the broker. Empty allows any pod in the cluster. |
| networkPolicy.egress | list | `[]` | Egress rules. Left to the cluster default unless set; the broker must reach its database, Redis, the vault backends and any alert destinations. |
| networkPolicy.enabled | bool | `false` | Create a NetworkPolicy restricting ingress to the broker. |
| nodeSelector | object | `{}` | Node selector for broker pods. |
| podAnnotations | object | `{}` | Extra annotations for broker pods. |
| podDisruptionBudget.enabled | bool | `true` | Create a PodDisruptionBudget so node drains keep at least minAvailable brokers up. |
| podDisruptionBudget.minAvailable | int | `1` | Minimum available replicas during voluntary disruption. |
| podLabels | object | `{}` | Extra labels for broker pods. |
| podSecurityContext | object | `{"fsGroup":10001,"runAsGroup":10001,"runAsNonRoot":true,"runAsUser":10001,"seccompProfile":{"type":"RuntimeDefault"}}` | Pod security context. The image ships a non-root user (uid 10001) and needs no write access to its root filesystem. |
| replicaCount | int | `2` | Number of broker replicas. Ignored when autoscaling.enabled is true. |
| resources | object | `{"limits":{"memory":"512Mi"},"requests":{"cpu":"100m","memory":"256Mi"}}` | Container resources. The broker is I/O bound; memory is bounded, CPU is left unlimited by default. |
| secret.adminPassword | string | `""` | Bootstrap password for the admin account. Only used when create is true. |
| secret.create | bool | `false` | Render a Secret from the values below. Development convenience only: the values end up in Git. |
| secret.databaseUrl | string | `""` | PostgreSQL DSN (postgresql://user:pass@host/db). Only used when create is true. |
| secret.existingSecret | string | `""` | Name of an existing Secret with keys database-url, redis-url, admin-password and optionally secret-key. Required unless create is true. |
| secret.redisUrl | string | `""` | Redis DSN (redis://host:6379). Only used when create is true. |
| secret.secretKey | string | `""` | Keys the scan-finding dedupe hash. Optional; set it so findings dedupe consistently across replicas and restarts. Only used when create is true. |
| securityContext | object | `{"allowPrivilegeEscalation":false,"capabilities":{"drop":["ALL"]},"readOnlyRootFilesystem":true}` | Container security context. |
| service.annotations | object | `{}` | Annotations for the Service. |
| service.port | int | `8080` | Service port; the container always listens on 8080. |
| service.type | string | `"ClusterIP"` | Service type. |
| serviceAccount.annotations | object | `{}` | Annotations for the ServiceAccount, e.g. eks.amazonaws.com/role-arn for IRSA. |
| serviceAccount.create | bool | `true` | Create a ServiceAccount for the broker pods. |
| serviceAccount.name | string | `""` | Name of an existing ServiceAccount to use when create is false. |
| serviceMonitor.enabled | bool | `false` | Create a Prometheus Operator ServiceMonitor for /metrics. |
| serviceMonitor.interval | string | `"30s"` | Scrape interval. |
| serviceMonitor.labels | object | `{}` | Extra labels, e.g. to match a Prometheus serviceMonitorSelector. |
| tolerations | list | `[]` | Tolerations for broker pods. |
| topologySpreadConstraints | list | `[]` | Topology spread constraints for broker pods. |
